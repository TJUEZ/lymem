# lymem 记忆服务接入说明（给 Agent 看的版本）

如果你（Agent）的运行环境里有本文件，说明你已接入 lymem 记忆服务。
这是你的长期记忆的后端。按下述规则使用它。

## 服务地址

- 记忆 API：`http://127.0.0.1:8801/api/v1`
- 嵌入代理：`http://127.0.0.1:8801/v1/embeddings`（OpenAI 兼容）
- 健康检查：`http://127.0.0.1:8801/api/v1/health`

所有请求不需要鉴权（本机回环）。服务不可达时静默降级，不要因记忆失败中断主任务。

## 何时写入记忆

满足任一条件就写，一次一条，不要攒批：

1. 工具执行产生了**可复用**的结果或做法（命令、参数、成功路径、踩坑）
2. 用户表达了**偏好**（格式、习惯、好恶、"以后都……"）
3. 用户**纠正了你**——这是最高价值的记忆
4. 出现了**事实变更**（换城市、换工具、项目状态变化）
5. 项目里程碑或重要结论

**不要写**：一次性调试输出、大段文件原文、敏感信息原文（证件号/密钥，写脱敏形式）、
你能随时从外部数据源重新取得的内容。

## 如何写入

```http
POST /api/v1/memories
{"events": [{"type": "conversation",
  "data": {"role": "user", "text": "以后纪要都用简体，落款右下角",
           "scene": "office", "source": "agent/<你的名字>"}}]}
```

- `type` 取：`tool_result` / `user_action` / `config` / `conversation`
- `scene` 用小写场景名：`office` / `dev` / `life`，不确定就不填
- 用户的原话优先原样保存，不要改写

## 如何读取

1. **任务开始时**：把任务描述作为 query 调检索，top-k 取 5~10
   ```http
   POST /api/v1/search {"query": "<任务描述>", "top_k": 8, "scenes": ["office"]}
   ```
   返回的记忆当作上下文引用，引用时注明来自记忆。
2. **偏好**：会话开始拉一次当前偏好；涉及历史问题时用
   `GET /api/v1/preferences/at?at=<ISO时间>` 查那个时点的偏好集合。
3. **冲突**：检索结果里同一事实出现多个值时，相信仲裁字段标记的现值；
   旧值在版本链里，需要时可查。

## 遗忘协议

用户要求"忘掉 X"时：

1. `POST /api/v1/forget/preview` 带上指令，展示将被遗忘的清单给用户确认
2. 确认后执行 `POST /api/v1/forget/exec`
3. 不要自作主张扩大遗忘范围；用户说忘"这条"就不要忘"这类"

## 偏好的使用规则

- 用户显式偏好 > 场景偏好 > 全局偏好
- 用户纠正你之后，除了当场照做，**必须**把纠正写回偏好（见"何时写入"第 2/3 条）
- 偏好过期被新版本替代是正常现象，不要把旧版本当作现行规则

## 边界

- 你只负责调 API；冲突仲裁、版本管理、遗忘的墓碑机制由 lymem 服务端完成，
  你不要在客户端自行"覆盖删除"
- 记忆检索结果与你的实时感知矛盾时，以实时为准，并把矛盾作为一条新记忆写入
  （服务端会走冲突管道）
